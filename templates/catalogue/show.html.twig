{% extends 'admin.html.twig' %}

{% block title %}Voir Catalogue 
{% endblock %}
{% block body %} <h1>Voir Catalogue </h1>
<div class='table-responsive'>
    <table class='table'>
<tr>
<th>Nom</th>
<td>{{catalogue.nom}}</td>
</tr>
<tr>
<th>Datestart</th>
<td>{{catalogue.datestart ? catalogue.datestart|date('Y-m-d H:i:s') : ''}}</td>
</tr>
<tr>
<th>Dateend</th>
<td>{{catalogue.dateend ? catalogue.dateend|date('Y-m-d H:i:s') : ''}}</td>
</tr>
<tr>
<th>Image</th>
<td>{{catalogue.image}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{catalogue.lien}}</td>
</tr>
<tr>
<th>Fichier</th>
<td>{{catalogue.fichier}}</td>
</tr><td>
    <form method='post' action="{{ path('catalogue_delete', {'id':  catalogue.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  catalogue . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('catalogue_copy', {'id':  catalogue.id }) }}">Dupliquer</a>
        <a href='{{ path('catalogue_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('catalogue_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('catalogue/_delete_form.html.twig') }}
    {% endblock %}