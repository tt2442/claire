{% extends 'admin.html.twig' %}

{% block title %}Voir Realisation 
{% endblock %}
{% block body %} <h1>Voir Realisation </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{realisation.categorie|json_encode}}</td>
</tr>
<td>{{realisation.materiels|json_encode}}</td>
</tr>
<tr>
<th>Titre</th>
<td>{{realisation.titre}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{realisation.description}}</td>
</tr>
<tr>
<th>Multimedia</th>
<td>{{realisation.multimedia}}</td>
</tr>
<tr>
<th>Etat</th>
<td>{{realisation.etat ? 'Yes' : 'No'}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{realisation.lien}}</td>
</tr>
<tr>
<th>Date</th>
<td>{{realisation.date ? realisation.date|date('Y-m-d H:i:s') : ''}}</td>
</tr><td>
    <form method='post' action="{{ path('realisation_delete', {'id':  realisation.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  realisation . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('realisation_copy', {'id':  realisation.id }) }}">Dupliquer</a>
        <a href='{{ path('realisation_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('realisation_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('realisation/_delete_form.html.twig') }}
    {% endblock %}