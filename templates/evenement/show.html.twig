{% extends 'admin.html.twig' %}

{% block title %}Voir Evenement 
{% endblock %}
{% block body %} <h1>Voir Evenement </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{evenement.materiels|json_encode}}</td>
</tr>
<tr>
<th>Titre</th>
<td>{{evenement.titre}}</td>
</tr>
<tr>
<th>Date</th>
<td>{{evenement.date ? evenement.date|date('Y-m-d H:i:s') : ''}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{evenement.description}}</td>
</tr>
<tr>
<th>Multimedia</th>
<td>{{evenement.multimedia}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{evenement.lien}}</td>
</tr>
<tr>
<th>Type</th>
<td>{{evenement.type}}</td>
</tr><td>
    <form method='post' action="{{ path('evenement_delete', {'id':  evenement.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  evenement . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('evenement_copy', {'id':  evenement.id }) }}">Dupliquer</a>
        <a href='{{ path('evenement_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('evenement_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('evenement/_delete_form.html.twig') }}
    {% endblock %}