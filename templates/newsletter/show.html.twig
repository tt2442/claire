{% extends 'admin.html.twig' %}

{% block title %}Voir Newsletter 
{% endblock %}
{% block body %} <h1>Voir Newsletter </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{newsletter.destinataires|json_encode}}</td>
</tr>
<tr>
<th>Object</th>
<td>{{newsletter.object}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{newsletter.description}}</td>
</tr>
<tr>
<th>Image</th>
<td>{{newsletter.image}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{newsletter.lien}}</td>
</tr>
<tr>
<th>Dateprog</th>
<td>{{newsletter.dateprog ? newsletter.dateprog|date('Y-m-d H:i:s') : ''}}</td>
</tr><td>
    <form method='post' action="{{ path('newsletter_delete', {'id':  newsletter.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  newsletter . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('newsletter_copy', {'id':  newsletter.id }) }}">Dupliquer</a>
        <a href='{{ path('newsletter_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('newsletter_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('newsletter/_delete_form.html.twig') }}
    {% endblock %}