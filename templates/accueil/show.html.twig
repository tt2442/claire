{% extends 'admin.html.twig' %}

{% block title %}Voir Accueil 
{% endblock %}
{% block body %} <h1>Voir Accueil </h1>
<div class='table-responsive'>
    <table class='table'>
<tr>
<th>Titre</th>
<td>{{accueil.titre}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{accueil.description}}</td>
</tr>
<tr>
<th>Image</th>
<td>{{accueil.image}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{accueil.lien}}</td>
</tr><td>
    <form method='post' action="{{ path('accueil_delete', {'id':  accueil.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  accueil . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('accueil_copy', {'id':  accueil.id }) }}">Dupliquer</a>
        <a href='{{ path('accueil_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('accueil_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('accueil/_delete_form.html.twig') }}
    {% endblock %}