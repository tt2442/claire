{% extends 'admin.html.twig' %}

{% block title %}Voir Reseausocial 
{% endblock %}
{% block body %} <h1>Voir Reseausocial </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{reseausocial.contact|json_encode}}</td>
</tr>
<tr>
<th>Nom</th>
<td>{{reseausocial.nom}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{reseausocial.lien}}</td>
</tr>
<tr>
<th>Pseudo</th>
<td>{{reseausocial.pseudo}}</td>
</tr><td>
    <form method='post' action="{{ path('reseausocial_delete', {'id':  reseausocial.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  reseausocial . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('reseausocial_copy', {'id':  reseausocial.id }) }}">Dupliquer</a>
        <a href='{{ path('reseausocial_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('reseausocial_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('reseausocial/_delete_form.html.twig') }}
    {% endblock %}