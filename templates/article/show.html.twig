{% extends 'admin.html.twig' %}

{% block title %}Voir Article 
{% endblock %}
{% block body %} <h1>Voir Article </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{article.categorie|json_encode}}</td>
</tr>
<tr>
<th>Nom</th>
<td>{{article.nom}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{article.description}}</td>
</tr>
<tr>
<th>Image</th>
<td>{{article.image}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{article.lien}}</td>
</tr>
<tr>
<th>Date</th>
<td>{{article.date ? article.date|date('Y-m-d H:i:s') : ''}}</td>
</tr><td>
    <form method='post' action="{{ path('article_delete', {'id':  article.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  article . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('article_copy', {'id':  article.id }) }}">Dupliquer</a>
        <a href='{{ path('article_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('article_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('article/_delete_form.html.twig') }}
    {% endblock %}