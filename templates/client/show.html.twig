{% extends 'admin.html.twig' %}

{% block title %}Voir Client 
{% endblock %}
{% block body %} <h1>Voir Client </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{client.newsletters|json_encode}}</td>
</tr>
<tr>
<th>Mail</th>
<td>{{client.mail}}</td>
</tr><td>
    <form method='post' action="{{ path('client_delete', {'id':  client.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  client . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('client_copy', {'id':  client.id }) }}">Dupliquer</a>
        <a href='{{ path('client_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('client_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('client/_delete_form.html.twig') }}
    {% endblock %}