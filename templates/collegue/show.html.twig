{% extends 'admin.html.twig' %}

{% block title %}Voir Collegue 
{% endblock %}
{% block body %} <h1>Voir Collegue </h1>
<div class='table-responsive'>
    <table class='table'>
<tr>
<th>Titre</th>
<td>{{collegue.titre}}</td>
</tr>
<tr>
<th>Description</th>
<td>{{collegue.description}}</td>
</tr>
<tr>
<th>Lien</th>
<td>{{collegue.lien}}</td>
</tr>
<tr>
<th>Fichier</th>
<td>{{collegue.fichier}}</td>
</tr>
<tr>
<th>Image</th>
<td>{{collegue.image}}</td>
</tr><td>
    <form method='post' action="{{ path('collegue_delete', {'id':  collegue.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  collegue . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('collegue_copy', {'id':  collegue.id }) }}">Dupliquer</a>
        <a href='{{ path('collegue_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('collegue_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('collegue/_delete_form.html.twig') }}
    {% endblock %}