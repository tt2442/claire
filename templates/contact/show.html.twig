{% extends 'admin.html.twig' %}

{% block title %}Voir Contact 
{% endblock %}
{% block body %} <h1>Voir Contact </h1>
<div class='table-responsive'>
    <table class='table'>
<td>{{contact.reseau|json_encode}}</td>
</tr>
<tr>
<th>Mail</th>
<td>{{contact.mail}}</td>
</tr>
<tr>
<th>Tel</th>
<td>{{contact.tel}}</td>
</tr><td>
    <form method='post' action="{{ path('contact_delete', {'id':  contact.id }) }}" onsubmit="return confirm('Etes-vous sûr de vouloir effacer cet item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~  contact . id ) }}">
        <a class='btn btn-xs btn-secondary' data-toggle='tooltip' title='Dupliquer' href="{{ path('contact_copy', {'id':  contact.id }) }}">Dupliquer</a>
        <a href='{{ path('contact_index') }}'><button class='btn btn-secondary mr-2' type='button'>Revenir à la liste</button></a>
        <button class="btn btn-xs btn-warning ">Supprimer</button>
    </form>
    </tr>
    </tbody>
    </table>
    </div><a href="{{ path('contact_new') }}"><button class="btn btn-secondary" type="button">Ajouter</button></a>

{{ include('contact/_delete_form.html.twig') }}
    {% endblock %}